#include <stdio.h>
int main(int argc, char *argv[]) {
	int a,b;
	printf("Enter the values a=");
	scanf("%d",&a);
	printf("\nEnter the values b1=");
	scanf("%d",&b);
	printf("The address value of a stored in %d ",&a);
	printf("\nThe address value of b stored in %d ",&b);
	printf("\nTHE SUM=%d",a+b);
	return 0;
}
